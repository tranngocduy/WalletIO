import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    overflow: 'hidden'
  },
  view: {
    flex: 1,
    backgroundColor: 'transparent'
  },
  chart: {
    flexGrow: 1,
    marginBottom: -19,
    backgroundColor: 'transparent'
  }
});
